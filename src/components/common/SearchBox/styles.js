import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  searchBoxContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: 15,
    marginTop: 15,
    marginBottom: 15,
  },
  searchIcon: {
    width: 24,
    height: 24,
    marginRight: 10,
  },
  searchText: {
    flex: 1,
    fontSize: 16,
  },
});

export default styles;
